/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    // distDir: "build",
    // output: "export",
    // basePath: "/app/next"
  };

export default nextConfig;
